import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta = {
  title: 'Reservations/ReservationForm',
  component: () => <div>ReservationForm placeholder</div>,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
