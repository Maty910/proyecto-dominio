{
  "name": "hotel-reservas",
  "private": true,
  "version": "1.0.0",
  
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
